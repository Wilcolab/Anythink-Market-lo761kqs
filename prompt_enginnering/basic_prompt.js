/*
Prompt: Write a JavaScript function `toCamelCase(str)` that converts an input string to camelCase.

Requirements:
- Preserve letters and digits; remove or convert other punctuation to word boundaries.
- Treat spaces, hyphens, underscores, and dots as word separators.
- Collapse repeated separators and trim surrounding whitespace.
- The first word should be all lowercase; subsequent words should be capitalized (Pascal-style for internal words).
- Return an empty string for null/undefined/empty input.

Example inputs and expected outputs:
- Input: "hello world" -> Output: "helloWorld"
- Input: "  Foo-bar_baz.qux " -> Output: "fooBarBazQux"
- Input: "alreadyCamelCase" -> Output: "alreadyCamelCase"

Please provide only the function implementation (no extra logging), well-formatted and robust for typical string inputs.
*/

